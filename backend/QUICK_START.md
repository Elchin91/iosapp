# 🚀 Quick Start Guide - M10 Backend

Этот гайд поможет вам запустить backend за 5 минут!

## Вариант 1: Самый простой (без базы данных)

### 1. Установка Python
Убедитесь, что у вас установлен Python 3.8+:
```bash
python --version
```

### 2. Установка зависимостей
```bash
cd backend
pip install -r requirements_simple.txt
```

### 3. Запуск сервера
```bash
python main_simple.py
```

Сервер запустится на `http://localhost:8000`

✅ **Готово!** Теперь можно тестировать приложение.

## Вариант 2: С Docker (рекомендуется)

### 1. Установка Docker
- Windows/Mac: Скачайте [Docker Desktop](https://www.docker.com/products/docker-desktop)
- Linux: `curl -fsSL https://get.docker.com | sh`

### 2. Запуск
```bash
cd backend
docker-compose up -d
```

### 3. Проверка
```bash
docker-compose ps
```

## Тестирование API

### 1. Проверка здоровья
Откройте в браузере: http://localhost:8000

### 2. API документация
Откройте: http://localhost:8000/docs

### 3. Создание сессии (curl)
```bash
curl -X POST http://localhost:8000/api/v1/chat/ios/session
```

### 4. Отправка сообщения
```bash
curl -X POST http://localhost:8000/api/v1/chat/ios/message \
  -H "Content-Type: application/json" \
  -d '{
    "session_id": "YOUR_SESSION_ID",
    "message": "Salam, BakıKART haqqında məlumat ver",
    "timestamp": "2024-01-01T00:00:00Z",
    "device_info": {
      "model": "iPhone",
      "os_version": "17.0",
      "app_version": "1.0"
    }
  }'
```

## Настройка iOS приложения

### 1. Найдите ваш IP адрес

**Windows:**
```cmd
ipconfig
```
Ищите "IPv4 Address"

**Mac/Linux:**
```bash
ifconfig | grep inet
```

### 2. Обновите APIConfig.swift

```swift
// Для локального тестирования
static let baseURL = "http://YOUR_IP_ADDRESS:8000/api/v1"  // Например: http://192.168.1.100:8000/api/v1
static let useMockMode = false  // Переключитесь на реальный API
```

⚠️ **Важно**: Используйте IP адрес вашего компьютера, а не localhost!

## Развертывание в интернете (бесплатно)

### Вариант 1: Railway.app (самый простой)

1. Зарегистрируйтесь на [Railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Railway автоматически развернет ваш backend
5. Получите URL вида: `https://your-app.railway.app`

### Вариант 2: Render.com

1. Зарегистрируйтесь на [Render.com](https://render.com)
2. New > Web Service
3. Подключите GitHub
4. Настройки:
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `uvicorn main:app --host 0.0.0.0 --port $PORT`

### Вариант 3: Heroku

1. Создайте файл `Procfile`:
```
web: uvicorn main:app --host 0.0.0.0 --port $PORT
```

2. Деплой:
```bash
heroku create your-app-name
git push heroku main
```

## Проблемы и решения

### Ошибка: "Connection refused"
- Проверьте, что backend запущен
- Используйте IP адрес, а не localhost
- Проверьте firewall

### Ошибка: "Kimi API error"
- Проверьте API ключ в `.env`
- Проверьте интернет соединение

### iOS приложение не подключается
- Убедитесь, что телефон и компьютер в одной сети
- Проверьте правильность IP адреса
- Для iOS 14+ добавьте в Info.plist:
```xml
<key>NSAppTransportSecurity</key>
<dict>
    <key>NSAllowsArbitraryLoads</key>
    <true/>
</dict>
```

## Следующие шаги

1. ✅ Запустите backend
2. ✅ Обновите IP адрес в iOS приложении
3. ✅ Протестируйте на реальном устройстве
4. 🚀 Разверните в интернете для публичного доступа

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs`
2. Перезапустите: `docker-compose restart`
3. Обратитесь к документации в папке `@Ai Confluence`
