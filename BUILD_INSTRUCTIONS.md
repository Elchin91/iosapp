# 📱 M10Clone - Инструкции по сборке IPA

## 🚀 Автоматическая сборка через GitHub Actions (Рекомендуется)

### Шаг 1: Загрузите проект на GitHub

```bash
cd "c:\Users\elchi\Desktop\IOS app\M10Clone"
git add .
git commit -m "Initial commit: M10Clone iOS app with AI chat"
git branch -M main
git remote add origin https://github.com/Elchin91/iosapp.git
git push -u origin main
```

### Шаг 2: GitHub Actions автоматически соберет .ipa

1. Зайдите на https://github.com/Elchin91/iosapp/actions
2. Дождитесь завершения сборки (≈10-15 минут)
3. Скачайте .ipa файл из **Artifacts** → **M10Clone-IPA**

### Шаг 3: Скачайте IPA файл

После успешной сборки:
- Перейдите в Actions → последний workflow run
- Внизу страницы найдите **Artifacts**
- Скачайте **M10Clone-IPA.zip**
- Распакуйте и получите **M10Clone.ipa**

---

## 📲 Установка на iPhone через TrollStore

### Требования:
- iPhone с iOS 15.0 - 16.6.1 (для TrollStore 2)
- Установленный TrollStore

### Инструкция:

1. **Передайте .ipa на iPhone:**
   - AirDrop
   - iCloud Drive
   - Telegram/WhatsApp (отправьте себе)
   - Или любой другой способ

2. **Откройте файл:**
   - Нажмите на M10Clone.ipa
   - Выберите "Open in TrollStore"

3. **Установите:**
   - TrollStore откроется автоматически
   - Нажмите "Install"
   - Приложение установится БЕЗ сертификата разработчика!

4. **Готово!**
   - Иконка M10 Clone появится на домашнем экране
   - Запустите приложение
   - Перейдите на вкладку AI ✨ и протестируйте чат!

---

## 🛠️ Локальная сборка на macOS (для разработчиков)

### Требования:
- macOS с Xcode 15.0+
- Homebrew

### Шаги:

1. **Установите XcodeGen:**
   ```bash
   brew install xcodegen
   ```

2. **Сгенерируйте Xcode проект:**
   ```bash
   cd /path/to/M10Clone
   xcodegen generate
   ```

3. **Откройте проект:**
   ```bash
   open M10Clone.xcodeproj
   ```

4. **Соберите в Xcode:**
   - Product → Build (Cmd+B)
   - Product → Archive
   - Distribute App → Development → Export

5. **Получите .ipa:**
   - Xcode создаст .ipa в выбранной папке

---

## 🔧 Ручная сборка через xcodebuild (для опытных)

```bash
# Установите XcodeGen
brew install xcodegen

# Сгенерируйте проект
xcodegen generate

# Соберите приложение
xcodebuild clean build \
  -project M10Clone.xcodeproj \
  -scheme M10Clone \
  -configuration Release \
  -sdk iphoneos \
  -derivedDataPath DerivedData \
  CODE_SIGN_IDENTITY="" \
  CODE_SIGNING_REQUIRED=NO \
  CODE_SIGNING_ALLOWED=NO

# Создайте IPA
mkdir -p Payload
cp -r DerivedData/Build/Products/Release-iphoneos/M10Clone.app Payload/
zip -r M10Clone.ipa Payload
```

---

## 📦 Создание Release с тегом (опционально)

Чтобы GitHub Actions автоматически создал Release:

```bash
git tag v1.0.0
git push origin v1.0.0
```

GitHub создаст Release и прикрепит .ipa файл автоматически!

---

## 🐛 Troubleshooting

### "Сборка не запускается в GitHub Actions"
- Убедитесь, что workflow файл находится в `.github/workflows/build-ipa.yml`
- Проверьте вкладку Actions в GitHub - она должна быть включена

### "Сборка падает с ошибкой"
- Проверьте логи в Actions
- Убедитесь, что все Swift файлы синтаксически корректны
- Проверьте, что `project.yml` правильно настроен

### "TrollStore не открывает .ipa"
- Убедитесь, что ваша версия iOS поддерживается TrollStore
- Попробуйте переустановить TrollStore
- Проверьте, что .ipa файл не поврежден

### "Приложение крашится при запуске"
- Это нормально для unsigned сборки без реального бэкенда
- AI чат должен работать
- Другие функции могут требовать доработки

---

## 📊 Структура проекта

```
M10Clone/
├── .github/
│   └── workflows/
│       └── build-ipa.yml          # GitHub Actions workflow
├── M10Clone/
│   ├── M10CloneApp.swift          # Entry point
│   ├── ContentView.swift          # Main TabView
│   ├── Info.plist                 # App configuration
│   ├── Views/
│   │   ├── AIChatView.swift       # ⭐ AI Chat interface
│   │   ├── HomeView.swift
│   │   ├── PaymentsView.swift
│   │   ├── TransfersView.swift
│   │   └── ProfileView.swift
│   ├── ViewModels/
│   │   └── AIChatViewModel.swift
│   ├── Models/
│   │   └── Message.swift
│   └── Extensions/
│       └── Colors.swift            # M10 purple theme
├── project.yml                     # XcodeGen config
├── .gitignore                      # Git ignore rules
└── BUILD_INSTRUCTIONS.md           # This file
```

---

## 🎨 Особенности приложения

### ✅ Реализовано:
- ✨ **AI Chat** - полноценный чат-интерфейс с умным ассистентом
- 🏠 **Home** - баланс, быстрые действия, история транзакций
- 💳 **Payments** - категории услуг, поиск, недавние платежи
- 💸 **Transfers** - переводы по телефону/карте/счету
- 👤 **Profile** - управление картами, настройки, информация
- 🎨 **M10 Design** - фиолетовая цветовая схема (#7B3FF2)

### 📝 Требует доработки:
- Интеграция с реальным AI API (OpenAI, Claude, etc.)
- Backend для платежей и переводов
- Аутентификация пользователей
- Core Data для персистентности
- Push-уведомления

---

## 💡 Следующие шаги

1. **Протестируйте AI Chat:**
   - Откройте вкладку AI ✨
   - Напишите "Привет", "Какой баланс?", "Помощь"
   - AI ответит через 1.5 секунды!

2. **Кастомизируйте дизайн:**
   - Измените цвета в `Extensions/Colors.swift`
   - Добавьте логотип в Assets.xcassets

3. **Добавьте функциональность:**
   - Интегрируйте OpenAI API в `AIChatViewModel.swift`
   - Подключите реальный backend
   - Добавьте авторизацию

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте раздел Troubleshooting выше
2. Посмотрите логи GitHub Actions
3. Создайте Issue в репозитории

---

**Удачи с установкой! 🚀**
